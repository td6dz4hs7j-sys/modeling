# Q3 overlay P2 独立验收

**结论：PASS。** 本次复验基于最后一次 `run_problem3` / `overlayfinalize` 冻结产物；MATLAB 运行退出码 0，`fresh_main.log` 末尾为 `Q2 OVERLAY PASS`。验收为只读，没有改动求解文件或交付工作簿；本报告是本轮唯一新增文件。

## 范围与输入快照

- 指定 Q2 工作簿：`results/q2_strategy_scenarios/Q2_综合均衡最终结果.xlsx`，SHA-256 `1497d5058b2622da466e2b5378c3accc83f7c327495a4728094f663d8a4f6ab1`。
- Q2 MAT：`results/q2_strategy_scenarios/balanced_best_pass.mat`，SHA-256 `e3ff4a329ee6c01730d2aeec47159755d5dd06fc83d1ffdf4cdcbeacf214074a`。
- 两个哈希与复现清单一致；清单已包含输入、代码与10项已验证输出的哈希。最终工作簿 SHA-256 `2866e8d227688c7b7bc7e21101b96eff17253865da28f1cee6e989e7bc15168d`，与清单吻合。
- 参数 `g=9.806 m/s²`。源 Q2 验证为 PASS：23 个运输架次、80 箱、返航 `5862.352303 s`、运输能耗 `65.869091416 kWh`。

## 证据与交叉核对

- `Q3_结果提交.xlsx` 与交付 CSV 对应：23 架次运输、80 条逐箱交付、4 条非空中继任务、296 条连续通信证书区间；保留原工作簿的六个工作表；未发现 Excel 错误单元格。
- 与指定 Q2 工作簿逐行对照，所有架次的箱组、访问服务区顺序、机型和单架次能耗均相同。运输使用8架同型实体机和14组同型电池；相对原 Q2，11 个架次换同型实体机、14 个架次换同型电池、19 个起飞时刻改变。没有新增运输架次。
- 最终数值：联合返航 `5933.082181 s = 98.8847030 min`；运输能耗 `65.869091416 kWh`；中继能耗 `2.741650264 kWh`；合计 `68.610741681 kWh`；加权迟交 `W=0`。
- 中继任务由 `R01/R02` 两架执行，共4次，使用4个不同的中继能源组件；该组件池与运输电池分离。模型及验证器未要求所有中继先完成才启动运输。运输和中继作业区间有实际重叠，符合并行叠加口径。
- `continuous_independent_audit.json` 为 PASS：189 个运输飞行阶段、296 个区间，最大未覆盖时长 `0 s`，最小认证链路裕量 `0.004206344 dB`；证书方法为区间界验证，无时间采样。
- `validator_PASS.txt` 记录 `validate_q3_solution: PASS`。`solver_stats.json` 显示 MATLAB `intlinprog` exitflag 1、相对/绝对 gap 0、27 个节点、约束违反量 `7.28e-12`。候选输入中的种子时刻已明确标记为由 MATLAB 细化；最终排程应以 `transport_sorties.csv`、`relay_sorties.csv`、`Q3_结果提交.xlsx` 为准。
- 我复看了正式 `raw_q3_*`、`process_q3_*`、`result_q3_*`、`flow_overall_model` 的彩色图和灰度图。流程图重排后无文本溢出；航线、覆盖、能耗及中继服务图的标题、图例和坐标可读，灰度版仍能区分主要系列。审计中 DEM 地形图的 SVG 含栅格底图，这是地图栅格数据所需，并非额外装饰性嵌图。

## 模型与范围复核

- [q3_overlay_schedule.m](../../q3/q3_overlay_schedule.m) 的 fixedCandidate 路径将 Q2 路线、箱组、访问顺序和机型传入调度，只接受候选中同型 UAV/电池分配，并优化时间与中继服务时段；没有生成新的运输路线或架次。候选点/运输资源分配固定时，报告的零 gap 只代表该受限 MILP 的最优性。
- [q3_reorder_transport.m](../../q3/q3_reorder_transport.m) 将准备起点放在同机上一架次起飞后可开始的位置，将装载起点放在前一架次返航后；最终起飞取目标时刻、准备完成、装载完成、实体机返航和电池真实充电就绪时刻的约束结果。随后验证器按继承的 Q2 预准备规则独立校验，未把准备和装载时长重复叠加。
- [validate_q3_solution.m](../../q3/validate_q3_solution.m) 在本路径以 `InheritQ2Prestage=true` 调用 Q2 validator，验证实际路线能耗、截止时间、实体机/电池不重叠及满电复用，并执行中继物理与连续通信审计。两架中继机、组件唯一分配、服务和返航周转约束均由验证路径检查。
- 结果说明将解限定为固定 Q2 核心路线与机型、允许同型资源重排的已验证可行方案；没有宣称全局最优。MATLAB 的 `C` 零 gap 只适用于已选定的覆盖点及候选运输资源配置/搜索范围，不能外推到所有站点、资源分配和连续空间方案。

## P0 / P1 / P2 发现

- **P0：无。**
- **P1：无。** 原先流程图文字重叠问题已修复并复验；旧的“全部资源编号冻结”表述也已更新为核心路线/机型固定、允许同型实体与电池重排。
- **P2：** 连续证书的最小链路裕量为正但较小（`0.004206344 dB`），独立区间证明通过；该余量说明结果贴近物理可行边界，工程部署应关注地形/坐标/时钟误差，但不影响本次题目模型下的 PASS。搜索范围有限，`C` 的最优性仅为受限子问题结论。

**返工：** 无阻断返工项。保留正裕量较小和受限搜索两项边界说明即可。
