# 协作代理约束

本文件适用于所有在本仓库中工作的成员和代理。

## 开始任务前

- 读取 `docs/项目进度.md`、`docs/模型与参数.md` 和 `docs/分工.md`。
- 检查仓库状态、当前分支、远端和待提交修改：至少运行 `git status`，必要时运行 `git log` 与 `git diff`。
- 确认任务边界、输入文件、输出文件和验证方式；不把未确认的题目要求当作事实。

## 分支与 Pull Request

- 每个人在独立任务分支上工作，通过 Pull Request 汇总到 `main`。
- 不直接覆盖他人的修改，不强制推送，不自动合并 PR。
- 分支和 PR 应使用清楚的成员、任务和目的描述；冲突应通过沟通和人工解决。

## 数据、题目与事实

- 题目原始附件保持只读；如需修改模板，先复制到项目目录并说明来源。
- 不虚构数据、运行结果、图表、模型性能或参考文献。
- 所有结果必须来自实际运行，来源和关键参数必须可追溯。
- 竞赛届次、官方规则、截止时间和提交约束在核验前统一标为“待核验”。

## 公共接口变更

- 修改公共模型、参数、数据格式或数据接口时，必须在 PR 中说明变更内容、影响范围、兼容性和验证结果。
- 任何会影响其他成员任务的改动，都应在对应的 `docs/交接记录/` 中留下记录。

## 任务结束前

- 记录本次改动、运行过的验证命令及其结果。
- 记录仍未解决的问题、风险、阻塞和下一步建议。
- 更新必要的进度和交接记录，并确认工作区没有意外生成的临时文件。

## 密钥与本地环境

- 密钥、令牌、密码、私钥及本地环境文件不得提交。
- `.env`、凭据文件、缓存、虚拟环境和临时文件遵循 `.gitignore`；需要共享的配置使用脱敏的示例文件。
