# 2026 年中国研究生数学建模竞赛 D 题：问题一建模分析报告

> 本轮只处理问题一：固定服务区内的单点往返运输与不可拆货箱组批；不处理问题二至四，不生成完整竞赛论文。真实运行结果均带有能耗模型条件，不能脱离 docs/能耗模型核验.md 单独引用。

## 1. 题目与输入核验

题面规定一个调度中心 O01、15 个服务区 S_i、80 个不可拆货箱和 3 种运输机型。问题一的每个架次是单服务区直接往返 O01 → S_i → O01，同一服务区允许多架次，禁止跨服务区组批；问题一不考虑 8 架实体运输无人机和共享电池的跨架次调度。

用户提供的 D题.zip 已核验 SHA-256：

    BBB4C17705B70B1BA365523AB189810305DB86D6457AED5F4F5587274E4D07B4

输入契约实际通过：节点 16 个、逐箱需求 80 条、运输机型 3 类，DEM 为 EPSG:4326 GeoTIFF。地理说明 PDF 给出的 DEM 范围和有效高程与栅格元数据一致。结果运行所用文件哈希详见 results/运行状态.json。

## 2. 事实、来源与未展开内容

### 2.1 题面明确内容

- 两节点采用水平直线；巡航海拔是航段最高 DEM 地面高程以上 50 m。
- O01 作业高度取地面高程；服务区作业高度取地面高程以上 30 m。
- 每个航段按爬升、水平巡航、下降计算时间。
- 题面给出等效航程、时间、总能耗分解和返航安全余量公式。
- 下降能耗效率取 0，不单独计算下降附加能耗。

### 2.2 未能在题面中核验的内容

题目 DOCX 的附录 2 只写出 E=E_hor+E_up，没有展开 E_hor 和 E_up 的计算式。其 XML 公式对象也只包含这两个子项的符号，没有隐藏的功率、质量或效率表达式。运输无人机表虽给出空载质量和爬升效率，但没有旋翼数、桨盘面积、巡航功率等完整旋翼模型参数。

因此，不能把外部文献的某个公式写成题面公式，也不能用默认功率/电压/旋翼参数补齐。

### 2.3 本轮采用的条件性参考模型 A

为先跑通可验证的物理链和 baseline，使用 src.problem_energy_provider 登记的参数化模型：

    E_hor_gij(q) = d_ij * E_g^use / L_g(q)
    E_up_gij(q) = (m_g^empty + q) * g * h_ij^+
                  / (eta_g^up * 3.6e6)

它的依据是：题面提供了载荷相关的标准航程、可用电量、空载总质量和爬升能耗效率；第一式把标准航程解释为该载荷下可用电量对应的平均水平航程，第二式为重力势能除以转换效率。该模型可复核、量纲一致，但仍是建模假设，而非题面已经给出的唯一答案。外部参考只用于判断模型边界，不替换题目参数；核验记录见 docs/能耗模型核验.md。

## 3. 问题一数学模型

### 3.1 路线几何与时间

记服务区 i 的地面高程为 z_i，O01 地面高程为 z_0。对 O01—S_i 直线段，令 z^max_i 为 DEM 采样到的最高地面高程，巡航海拔为 z^max_i+50。两条航段的爬升/下降高度由起终点作业高度和该巡航海拔的差值取非负值。

对机型 g：

    t_gij = h_ij^+ / v_g^up + d_ij / v_g^c + h_ij^- / v_g^down

返航段货物已投放，取剩余载荷 q=0；这只影响能耗，不把交接时间或题面未给出的额外时延自行加入 Q1。

### 3.2 最大安全载荷

题面等效航程：

    L_g(q) = L_g^0 - (L_g^0 - L_g^F) * (q / Q_g)^(3/2)

对机型 g、服务区 i，程序在 [0,Q_g] 上检查：

    E_out(q) + E_back(0) <= (1-rho_g) * E_g^use
    d_out + d_back <= L_g(q)

先检查零载荷，再检查满载；若端点不满足则二分并在结果端点再次复核。安全载荷不超过额定最大载货质量，也不替代最终逐架次能耗验证。

### 3.3 固定服务区组批

令 B_i 为服务区 i 的货箱集合，w_b、v_b 为箱体质量和体积。每个架次只能属于一个服务区和一种机型，箱体不可拆且满足：

    sum_r x_br = 1
    sum_b w_b*x_br <= q_safe(g,i)*y_r
    sum_b v_b*x_br <= V_g*y_r

程序对每个候选架次再次调用完整能耗函数，检查返航余量和等效航程；不使用只按额定载荷的替代结果。

### 3.4 先 baseline，再比较改进方法

1. FFD baseline：各服务区内按质量/体积压力递减排序，放入第一个可行架次；无法放入时，开一个能承运该箱体的最小机型。它是确定性、可解释、可复核的基线，不宣称最优。
2. 局部搜索：只在同一服务区内做 relocate/swap，每一步都重新计算质量、体积、能量和航程，仅接受 (架次数, 总能耗, 累计时间) 字典序改善。
3. MILP 对照：用质量、体积和安全载荷容量建立最小架次 0-1 模型，得到解后再经过完整物理约束验证；若求解器不可用或没有可用解，保留 baseline 并记录原因。
4. 全程不使用遗传算法。Q1 只有固定服务区内组批，不能把跨区联合装箱或多点路径优化混入当前模型。

## 4. 真实数据运行结果（参考模型 A 条件下）

| 方法 | 架次数 | 总运输能耗（kWh） | 累计作业时间（s） | 约束验证 |
| --- | ---: | ---: | ---: | --- |
| FFD baseline | 52 | 112.63554699 | 61086.45154 | 通过 |
| FFD 后局部搜索 | 52 | 112.15906406 | 61086.45154 | 通过 |
| MILP 最小架次对照 | 18 | 63.58226639 | 19426.76126 | 通过 |
| MILP 后局部搜索 | 18 | 63.05857757 | 19426.76126 | 通过 |

程序最终选择按字典序最优的 MILP 后局部搜索方案写入 results/q1_组批方案.csv。该表的返航 SOC 使用百分数，所有 80 个货箱恰好出现一次，且不跨服务区。

安全载荷结果写入 results/q1_安全载荷.csv：A 型在 15 个服务区均为 25 kg；B 型约为 28.833–30 kg；C 型约为 58.954–80 kg。完整逐服务区数值以 CSV 为准。

## 5. 验证与限制

已执行：

- 题目输入契约、唯一编号和单位/正值检查；
- DEM CRS、范围、路线最高地面高程和节点采样；
- FFD 独立验证后才进入局部搜索/MILP；
- 箱体覆盖唯一性、固定服务区、质量、体积、返航能量、等效航程和汇总一致性；
- 软件结构自检 python -m src.self_test_q1；
- 运行环境检查：Python 3.12.14，numpy/pandas/matplotlib/scipy/openpyxl/pillow 均已找到。

限制：

- 模型 A 的 E_hor/E_up 仍需队伍根据官方补充解释或合理性分析最终确认；
- 当前没有独立质检 Subagent，不能声称 math-modeling skill 要求的 M1/P1/P2 独立 PASS；
- Q1 的配送时限字段已读取但本轮没有加入题面未明确的统一排程约束；
- 不应把这些条件性结果直接粘贴为正式竞赛结论。
