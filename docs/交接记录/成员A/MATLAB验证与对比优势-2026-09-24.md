# MATLAB 验证与对比优势

## 范围

使用本机 MATLAB R2022a 对算法实际选出的 `results/q1_组批方案.csv`、`results/q1_方法比较.csv`、`results/q1_约束验证.json` 做独立数值复核，并把 `results/q1_对照方案_统一模型.json` 作为固定组批参考；不修改题目原始附件。

## 命令与环境

    matlab -batch "addpath('src'); matlab_q1_batch"

- MATLAB：R2022a，版本 `9.12.0.1884302`。
- 检查功能：数据读取、基础可视化；所需 MATLAB 工具箱检查通过。
- 随机种子：`rng(0, "twister")`。
- 输入哈希已写入 `results/matlab_q1_verification.json` 和 `results/复现清单.json`；MATLAB 最新运行日志为 `results/matlab_q1_latest.log`，状态文件为 `results/matlab_q1_batch_error.txt`（成功时内容为 `No error`）。

## 复核结果

- 算法：MILP 最小架次 → 同服务区 relocate/swap 局部搜索；选出方法名为 `local search from MILP`。
- 18 架次、80 个货箱，唯一货箱数为 80。
- 往返飞行时间：`19426.761258051094 s`。
- 装卸交接时间：`13584 s`。
- 总作业时间：`33010.7612580511 s = 9.169655905014194 h`。
- 总能耗：`63.05857757030072 kWh`。
- 算法方案逐架次作业时间分解、方法表与逐架次汇总均为机器精度一致。
- MATLAB 直接读取运输无人机附件四项时间参数后，算法方案装卸交接时间最大残差：`0 s`。
- Python 完整约束核验通过，MATLAB 复核 80 箱唯一覆盖。

## 对比优势

本方案算法与基线/固定组批参考在同一结果口径下比较：

| 参照方案 | 能耗降低 | 累计作业时间降低 | 架次降低 |
| --- | ---: | ---: | ---: |
| FFD baseline | 44.0154% | 63.0283% | 65.3846% |
| MILP minimum-sortie | 0.8236% | 0% | 0% |

固定组批参考的重算结果为 `59.121155561792094 kWh`、`32760.301126723785 s`；与本方案算法相比，算法多 `3.937422008508626 kWh`、多 `250.4601313273124 s`（`0.06957225870203122 h`），架次相同。该参考组批不是由本项目算法生成，不能据此替换算法方法名或宣称全局最优。

## 输出

- `results/matlab_q1_verification.json`
- `results/matlab_q1_method_comparison.csv`
- `results/matlab_q1_advantage.csv`
- `results/matlab_q1_reference_gap.csv`
- `figures/process_q1_matlab_time_reconciliation.svg/png`
- `figures/result_q1_matlab_method_comparison.svg/png`
- `figures/result_q1_matlab_advantage.svg/png`

彩色图和灰度预览已实际检查；格式审计和 q1 图覆盖审计返回 0。R2022a 不支持 `exportgraphics` 直接写 SVG，项目内 `utils/export_publication_figure.m` 使用 MATLAB 原生 `print -dsvg/-dpng` 兼容导出。
