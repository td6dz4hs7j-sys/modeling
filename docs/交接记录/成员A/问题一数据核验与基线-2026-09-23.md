# 问题一数据核验与 baseline

- 成员：成员A（占位）
- 日期：2026-09-23
- 分支：init/d-uav-guobaotegong-framework
- PR：https://github.com/td6dz4hs7j-sys/modeling/pull/1
- 输入文件与版本：用户提供 D题.zip，SHA-256 为 BBB4C17705B70B1BA365523AB189810305DB86D6457AED5F4F5587274E4D07B4；运行文件哈希见 results/运行状态.json

## 本次改动

- 重新按 ZIP 原始中文文件名解压到 data/raw/D题/，原始输入保持只读；
- 修正 XLSX 递归定位、中心/服务区分表读取和运输机型表尾部清单读取；
- 加入运输机型含电池空载质量、爬升/下降效率字段；
- 实现题面参数缺少子项展开式时的透明参考模型 A；
- 运行 Q1：FFD baseline → 约束验证 → 局部搜索，并与 MILP 进行对照；
- results/q1_组批方案.csv 选取字典序最优候选，返航 SOC 按百分比输出。

## 验证命令与结果

    python -m src.self_test_q1

结果：PASS；仅为结构等价自检，不是竞赛结果。

    python C:\Users\HoGaaiDik\.codex\skills\math-modeling\references\roles\编程手\scripts\check_env.py --features data visualization optimization excel image

结果：所有必需依赖已找到。

    python -m src.run_q1 --data-dir data --output-dir results --energy-provider src.problem_energy_provider --no-milp

结果：FFD 52 架次、局部搜索 52 架次；约束验证通过。

    python -m src.run_q1 --data-dir data --output-dir results --energy-provider src.problem_energy_provider --milp-time-limit-s 30

结果：MILP 18 架次，MILP 后局部搜索 18 架次；约束验证通过。完整指标见 results/q1_方法比较.csv。

## 未解决问题或风险

- 题面 DOCX 没有展开 E_hor、E_up，模型 A 是透明的建模假设，不是官方唯一公式；
- 当前没有独立质检 Subagent，不能标记 M1/P1/P2 独立 PASS；
- Q1 需求表中的配送时限尚未进入时序排程；
- 结果不应在模型 A 未被队伍确认前直接写入正式论文。

## 下一步

- 由成员B/C 独立复核题面公式、输入哈希和模型 A；
- 若取得官方能耗补充说明，替换 src/problem_energy_provider.py 并重新运行；
- 生成真实 raw/process/result 图表，写入复现清单后再做人工评审。
