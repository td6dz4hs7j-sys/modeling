# Q1 主表替换与作业时间

- 成员：成员A
- 日期：2026-09-24
- 分支：init/d-uav-guobaotegong-framework
- PR：https://github.com/td6dz4hs7j-sys/modeling/pull/1
- 输入：`results/q1_对照方案_统一模型.json`、`results/问题1_对照方案_统一模型_含作业时间.xlsx`
- 本次改动：将统一模型的 18 架次方案替换到 `results/结果提交模板_Q1_已填写_含作业时间.xlsx` 的 `Q1_单点组批`，新增 `作业时间（s）` 列；同步更新 `Q1_统一模型结果` 明细列。
- 作业时间口径：问题一每架次为 O01→S_i→O01 单点往返；总作业时间 = 往返飞行 + 固定准备 + 每箱装载 + 接收点基础交接 + 每箱交接，返程不重复装卸。数值来自统一模型路线、机型速度和运输无人机附件参数。
- 验证命令与结果：
  - `python -m compileall -q src`：通过。
  - `python -m src.replace_q1_submission ...`：通过。
  - Q1 行数 18、货箱 80 个；往返飞行时间为 19272.301126724 s，装卸交接为 13488 s，总作业时间为 32760.301126724 s；总能耗为 59.121155561792 kWh。
  - Q2–Q4 表头与原始模板一致；输出 XLSX ZIP/XML 校验通过。
- 未解决问题或风险：当前环境没有 Excel/WPS 客户端，未做桌面界面打开测试；作业时间暂不含尚未建模的等待、调度和通信协同时延。
- 下一步：成员复核官方 Q1 提交表是否需要保留“往返时间”或只保留“作业时间”，并确认小数位口径。
