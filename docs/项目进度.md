# 项目进度

## 项目基本信息

- 项目标识：D-UAV-guobaotegong
- 当前承载仓库：td6dz4hs7j-sys/modeling
- 题目：D题——山区洪涝灾害下无人机运输与通信协同优化
- 当前阶段：问题一真实数据链、FFD baseline、局部搜索和 MILP 对照
- 目标竞赛与届次：题目 DOCX 标注为 2026 年中国研究生数学建模竞赛 D 题；正式规则仍需队伍人工核验
- 当前分支：init/d-uav-guobaotegong-framework
- 初始化 PR：https://github.com/td6dz4hs7j-sys/modeling/pull/1

## 已完成

- [x] 确认仓库可读取、可写入，保留已有文件和未提交修改
- [x] 建立三人协作目录、README、AGENTS 和进度/模型/分工记录
- [x] 固定 math-modeling skill 版本、源仓库和 commit
- [x] 读取题目 DOCX，限定本轮只处理问题一
- [x] 核验用户提供 ZIP 的 SHA-256、条目结构和地理说明 PDF
- [x] 正确解压中文文件名，保留 data/raw/D题/ 只读快照
- [x] 通过输入契约：16 节点、80 货箱、3 机型、DEM 可采样
- [x] 建立题面等效航程/分阶段时间/返航余量与 DEM 路线计算
- [x] 建立显式参数化参考能耗模型 A，并标注其不是题面唯一展开式
- [x] 真实数据跑通 FFD baseline、局部搜索和 MILP 对照
- [x] 对所有候选方案执行逐箱唯一性、单服务区、质量、体积、航程、能量和汇总一致性检查
- [x] 按题目结果模板生成 `results/结果提交模板_Q1_已填写.xlsx`，保留 Q2–Q4 空表并加入 Q1 详细对比工作表
- [x] 对 Excel 工作簿执行表头、行数、模板工作表保留、公式错误和 CSV 数值容差核验
- [x] 为兼容不同 Excel/WPS 版本，移除附加工作表的结构化 Table 定义，改用普通筛选区域并重新验证 OOXML
- [x] 将外部 `问题1_结果提交.xlsx` 复制为只读快照，按 reference-model-A-v1 完成逐架次统一重算，并生成 Excel/CSV/JSON 对照结果
- [x] 将统一模型方案替换到 Q1 主表，并增加每架次 `作业时间（s）` 列；当前列已按附件参数包含准备、装载和接收点交接时间
- [x] 按题面附录 2 与 `运输无人机数据.xlsx` 明确区分往返飞行时间、装卸交接时间和总作业时间

## 当前真实运行结果（条件性）

以下结果来自 results/，采用 reference-model-A-v1，不是宣称的官方唯一答案：

| 方法 | 架次数 | 总运输能耗（kWh） | 累计作业时间（s） | 验证 |
| --- | ---: | ---: | ---: | --- |
| FFD baseline | 52 | 112.63554699 | 89286.45154 | 通过 |
| FFD 后局部搜索 | 52 | 112.15906406 | 89286.45154 | 通过 |
| MILP 最小架次对照 | 18 | 63.58226639 | 33010.76126 | 通过 |
| MILP 后局部搜索 | 18 | 63.05857757 | 33010.76126 | 通过 |

当前 `q1_组批方案.csv` 是程序独立搜索得到的候选方案（先架次数、再能耗、再总作业时间），不是下方外部 59.13 kWh 方案的逐架次重算结果；返航 SOC 已按百分数输出，明细同时保留往返飞行时间和总作业时间。结果必须在公式假设被队伍接受后才可用于论文草稿。

## 外部 Q1 方案统一模型重算（2026-09-24）

外部方案快照为 `data/raw/D题/对照方案_问题1_结果提交.xlsx`，SHA-256 为 `6BA869961E276BB833ED5BF5836E4A62016BD913EB11E235CF48FC3D7B888BB0`，原文件保持只读。逐架次按 `reference-model-A-v1` 重算，固定外部方案的服务区、箱体分组和机型，不重新优化：

| 指标 | 外部原表逐架次求和 | 统一模型重算 | 说明 |
| --- | ---: | ---: | ---: |
| 架次数 | 18 | 18 | 0 |
| 总运输能耗（kWh） | 59.23290000 | 59.12115556 | 统一模型 - 外部 = -0.11174444 |
| 往返飞行时间（s） | 19289.27000000 | 19272.30112672 | 统一模型 - 外部 = -16.96887328 |
| 装卸交接时间（s） | 未提供 | 13488.00000000 | 由附件逐架次参数求和 |
| 总作业时间（s） | 未提供 | 32760.30112672 | 往返飞行 + 装卸交接 |
| 总作业时间（h） | 未提供 | 9.10008365 | 秒换算为小时 |

统一模型核验通过：80 个货箱各出现一次、无跨服务区组批，质量、体积、返航能量余量和等效航程约束均通过。外部工作簿没有记录其原始公式、DEM 采样策略或未四舍五入中间值，因此不能把 0.11174444 kWh 的差异归因到某一个具体步骤；外部原表也没有提供可核验的装卸交接总时间，故不把 32,776 s 左右的外部截图时间当作统一模型输入。详细逐架次差异见 `results/q1_对照方案_统一模型.csv` 和带作业时间的工作簿。该结论仍受参考模型 A 的条件性假设约束。

## 仍需人工核验或后续完成

- [ ] 由队伍确认参数化参考模型 A 是否符合比赛解释；若有官方补充，替换适配器并重跑全部结果
- [ ] 比较不同水平能耗解释下的鲁棒性，不把单一条件性结果写成事实
- [x] 完成独立质检 Subagent：时间公式、逐架次合计和最终工作簿核验 PASS；成员仍需复核官方填写口径
- [x] 生成并检查 raw/process/result 图表和整体流程图；仍需队伍成员独立复核
- [x] 完成 results/复现清单.json 和数据核验/交接记录
- [ ] 队伍成员独立复核 Excel 提交列、保留小数位和题目官方填写口径
- [ ] 问题二至四、完整论文和正式提交格式仍未开始

## 复现命令

先跑 baseline：

    python -m src.run_q1 --data-dir data --output-dir results --energy-provider src.problem_energy_provider --no-milp

再跑 MILP 对照：

    python -m src.run_q1 --data-dir data --output-dir results --energy-provider src.problem_energy_provider --milp-time-limit-s 30

生成已填写的 Q1 Excel 模板：

    python -m src.fill_q1_excel --template data/raw/D题/结果提交模板.xlsx --data-dir data --results-dir results --output results/结果提交模板_Q1_已填写.xlsx

重算外部 Q1 方案：

    python -m src.recompute_q1_solution --solution data/raw/D题/对照方案_问题1_结果提交.xlsx --data-dir data --output-dir results --excel-output results/问题1_对照方案_统一模型_含作业时间.xlsx

替换主提交工作簿的 Q1：

    python -m src.replace_q1_submission --source-workbook results/问题1_对照方案_统一模型_含作业时间.xlsx --result-json results/q1_对照方案_统一模型.json --output results/结果提交模板_Q1_已填写_含作业时间.xlsx

也可用已验证的 artifact-tool 更新现有主工作簿中的 Q1 作业时间列：

    node src/update_q1_workbook.mjs

所有输入哈希和 provider source 会写入 results/运行状态.json；完整依赖、参数和命令还应写入 results/复现清单.json。若原输出工作簿被 Excel/WPS 占用，使用带“含作业时间”的新文件名，避免强制覆盖。

## Git 记录

- main 初始基线：eff4b930b71e7afeb4ff494a992ab6722aaa5eff
- 初始化提交：32ec9e34be2ecded82307ee3516b2d203468f245
- 初始化记录提交：a5e855bfcfcae9f94ab3383d8c145d4b568026dc
- Q1 模型与结果提交：4717775121d336eed3d138f4e51dac8761fdd0d3
